info:
  name: Continue Story
  type: graphql
  seq: 2

graphql:
  method: POST
  url: "{{BASE_URL}}/graphql"
  body:
    query: |
      mutation ContinueStory(
        $sessionId: String!
        $choiceId: String!
        $timeTakenMs: Int!
      ) {
        continueStory(
          sessionId: $sessionId
          choiceId: $choiceId
          timeTakenMs: $timeTakenMs
        ) {
          sessionId
          isEnding
          node {
            textContent
            choices {
              id
              text
            }
          }
        }
      }
    variables: |2-
      
       {
         "sessionId": "cmo8iho7h000001pexz230vlo",
         "choiceId": "cmo8kas760003hgq4m7wttn6l",
         "timeTakenMs": 5000.0
       }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
