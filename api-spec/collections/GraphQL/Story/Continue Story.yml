info:
  name: Continue Story
  type: graphql
  seq: 2

graphql:
  method: POST
  url: "{{BASE_URL}}/graphql"
  body:
    query: |
      mutation ContinueStory(
        $sessionId: String!
        $choiceId: String!
        $timeTakenMs: Int!
      ) {
        continueStory(
          sessionId: $sessionId
          choiceId: $choiceId
          timeTakenMs: $timeTakenMs
        ) {
          sessionId
          isEnding

          node {
            textContent
            imageUrl
            audioUrl
            confidenceScore
            choices {
              id
              text
            }
          }
        }
      }
    variables: |2-
      
       {
         "sessionId": "cmo8n5jnt0000kcq4q0ilek9p",
         "choiceId": "cmo8n8csv0007kcq4utavhgui",
         "timeTakenMs": 5000.0
       }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
