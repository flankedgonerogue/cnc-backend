meta {
  name: Start Story
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/story/start
  body: json
  auth: inherit
}

body:json {
  {
    "sessionId": "{{SESSION_ID}}",
    "childName": "Avery",
    "childAge": 7
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
