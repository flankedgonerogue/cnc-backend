meta {
  name: Update Template
  type: http
  seq: 2
}

patch {
  url: {{BASE_URL}}/templates/:templateId
  body: json
  auth: inherit
}

params:path {
  templateId: {{TEMPLATE_ID}}
}

body:json {
  {
    "promptSuggestion": "Keep it long tho.."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
