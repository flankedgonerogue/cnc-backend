meta {
  name: Get Template
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/templates/:templateId
  body: none
  auth: inherit
}

params:path {
  templateId: {{TEMPLATE_ID}}
}

settings {
  encodeUrl: true
  timeout: 0
}
