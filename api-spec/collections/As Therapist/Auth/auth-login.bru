meta {
  name: auth/login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{THERAPIST_EMAIL}}",
    "password": "{{DEFAULT_PASSWORD}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
