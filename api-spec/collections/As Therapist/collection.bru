auth {
  mode: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN_THERAPIST}}
}

script:post-response {
  const token = res.body.access_token
  if (token) {
    bru.setGlobalEnvVar("ACCESS_TOKEN_THERAPIST", token)
  }
}
