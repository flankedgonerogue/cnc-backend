meta {
  name: auth/verify
  type: http
  seq: 3
}

get {
  url: {{BASE_URL}}/auth/verify
  body: none
  auth: bearer
}

auth:bearer {
  token: {{THERAPIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
